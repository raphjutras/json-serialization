{
  "name": "@peerlancers/ts-object-mapper",
  "description": "A typescript library for JSON object serialization.",
  "version": "0.0.2",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "author": "Arrian Pascual",
  "license": "MIT",
  "keywords": ["object-mapper", "ts-object-mapper", "typescript mapper", "object mapper"],
  "scripts": {
    "test": "ts-node node_modules/jasmine/bin/jasmine --config=jasmine.json",
    "mapper:build": "tsc",
    "mapper:publish": "npm run mapper:build && npm run test && npm publish --access public",
    "execute": "npm run build && cd dist && node sample.js"
  },
  "typescript": {
    "definition": "index.d.ts"
  },
  "dependencies": {
    "reflect-metadata": "^0.1.13"
  },
  "devDependencies": {
    "@types/jasmine": "^3.4.0",
    "@types/node": "^12.7.4",
    "ts-node": "^8.3.0",
    "jasmine": "^3.4.0",
    "jasmine-spec-reporter": "^4.2.1",
    "typescript": "~3.4.3"
  }
}
